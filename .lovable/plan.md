Fix `/MNexperts` hero so the same image is not displayed twice.

1. Remove the foreground hero image card in the Minneapolis-specific expert hero.
2. Keep the existing photo only as the full hero background.
3. Let the hero text occupy the left side with the existing darker overlay for readability.
4. Leave the CTA behavior unchanged, including opening the expert card form.
5. Verify the page shows one hero photo, not a photo on top of the same photo.